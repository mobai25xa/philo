[package]
name = "philo-consumer-minimal-call"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
philo.workspace = true

[features]
default = ["rustls-tls"]
rustls-tls = ["philo/rustls-tls"]
tracing = ["philo/tracing"]
